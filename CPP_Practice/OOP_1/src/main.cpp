#include <iostream>
#include <string>

class Robot {
    // private: used to define parameters that are not accessible outside the class
private:
    std::string type;
    double batteryLevel;

    // public: used to define parameters that are accessible outside the class
public:
    
}